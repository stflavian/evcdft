name: EVC_DFT CI

on:
  push:
    branches: [ never ]
  pull_request:
    branches: [ never ]

jobs:
  test:
    name: Julia 1.12 on Ubuntu - DISABLED
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: This workflow is disabled
      run: echo "This workflow is disabled. Use quick-test.yml instead."
