name: EVC_DFT Test Suite

on:
  push:
    branches: [ never ]
  pull_request:
    branches: [ never ]

jobs:
  test:
    name: Run Tests (Julia 1.12) - DISABLED
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: This workflow is disabled
      run: echo "This workflow is disabled. Use quick-test.yml instead."
